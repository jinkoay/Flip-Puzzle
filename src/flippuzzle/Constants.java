package flippuzzle;

// Let's create a class to store the number of rows and columns in 
// the board's grid for easy modification in the future if need be.
public class Constants {
    public static final int NUMROWS = 3;
    public static final int NUMCOLS = 3;
    public static final int NUMTILES = NUMROWS * NUMCOLS;
}
